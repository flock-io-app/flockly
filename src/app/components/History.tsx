import React from "react";

interface HistoryProps {
  historyList: object[];
}
export const History: React.FC<HistoryProps> = ({ historyList }) => {
  return <div>history</div>;
};

export default History;
